// main.js — основной JavaScript-файл (пока минимальный)

// Эта функция выполнится, когда весь HTML будет загружен
document.addEventListener('DOMContentLoaded', function() {
    // Здесь можно будет добавить обработчики для AJAX-запросов
    console.log('VibeMatcher готов к работе');
});