# run.py — точка входа для запуска встроенным сервером Werkzeug (только для разработки!)
# Импортируем функцию create_app из нашего пакета app
from app import create_app

# Создаём объект приложения Flask, используя настройки по умолчанию
app = create_app()

# Если мы запускаем этот файл напрямую (python run.py), стартуем веб-сервер
if __name__ == '__main__':
    # debug=True включает отладчик (показывает подробные ошибки) и автоперезагрузку
    app.run(debug=True)